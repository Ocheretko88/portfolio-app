/**
 * Production environment (default). Used by `ng build` (Vercel).
 * `environment.development.ts` replaces this file for `ng serve`.
 */
export const environment = {
  production: true,
  apiUrl: 'https://portfolio-api-ldu9.onrender.com',
};
